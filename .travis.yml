language: php

php:
  - 7

cache:
  directories:
    - $HOME/.composer/cache

before_install:
  - ./tests/setup.sh
  - composer validate

install:
  - composer --prefer-source install

script:
  - ./vendor/bin/phpunit --colors
